export default function Task04(){
    return <p className="text-[20px] font-[500]">useTransition lets you mark state updates as non-urgent. While React processes the update, isPending becomes true so you can show a loading state.</p>
}