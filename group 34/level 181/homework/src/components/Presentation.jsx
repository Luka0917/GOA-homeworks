export default function Presentation(){
    return (
        <p className="text-[20px] font-[500] text-center">useReducer is a React Hook used to manage complex state logic by passing actions to a reducer function, which updates state based on the action type and payload—ideal for when state updates depend on previous state or involve multiple sub-values.</p>
    )
}